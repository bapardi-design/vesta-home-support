{
  "name": "nuahome-public-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build-static.mjs",
    "test": "node verify-static.mjs",
    "check": "npm run build && npm test"
  }
}
